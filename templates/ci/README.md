# CI Templates — installed by `/dev-kit:ci-setup`

This directory is the source-of-truth CI template tree. Running `/dev-kit:ci-setup`
copies it into a target project's `.github/`, `.githooks/`, and `scripts/`.

See `docs/ci-setup.md` in the dev-harness-kit plugin for the user guide.
