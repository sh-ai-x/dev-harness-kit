name: CI
on:
  pull_request:
    types: [opened]
jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps: [{run: echo}]
  review:
    name: review
    runs-on: ubuntu-latest
    steps: [{run: echo}]
